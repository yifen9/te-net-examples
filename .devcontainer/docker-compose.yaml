services:
  te-net-examples-dev:
    image: ghcr.io/yifen9/te-net-examples-dev
    build:
      context: ..
      dockerfile: Dockerfile.dev
    container_name: te-net-examples-dev
    working_dir: /workspace
    volumes:
      - "..:/workspace"
    command: >
      bash -lc "
        just init &&
        exec sleep infinity
      "
